Swish = {}
print(Framework)
Swish.Funcs = {
    SV = {
        RemoveBankMoney = function(identifier, amount) -- Remove money from bank account

        end,
        AddBankMoney = function(identifier, amount) -- Add money to bank account

        end,
        GetBankMoney = function(identifier) -- Get bank account balance

        end
    }
}